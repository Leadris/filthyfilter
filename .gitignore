# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log

# Dependencies / build (not used now — static site, but just in case)
node_modules/
dist/
.cache/

# Local scratch and release packaging. Deploy archives are build output, not
# source: they are rebuilt from any commit and each one weighs about 22 MB.
tmp/
